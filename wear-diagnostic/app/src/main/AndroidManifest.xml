<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.hardware.type.watch" android:required="false" />

    <application
        android:allowBackup="false"
        android:label="YTM Session Bridge"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SessionNotificationListener"
            android:exported="false"
            android:label="YTM Session Bridge"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- android.permission.DUMP is held by adb shell, not ordinary apps. -->
        <receiver
            android:name=".ShellCommandReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.asuma.ytmsessiondiagnostic.LIKE_CURRENT" />
                <action android:name="com.asuma.ytmsessiondiagnostic.STATUS_CURRENT" />
                <action android:name="com.asuma.ytmsessiondiagnostic.PREVIOUS" />
                <action android:name="com.asuma.ytmsessiondiagnostic.NEXT" />
                <action android:name="com.asuma.ytmsessiondiagnostic.PLAY_PAUSE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
